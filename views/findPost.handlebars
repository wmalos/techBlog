{{!-- <div class="row mb-4 post">
  <div class="col-md-5">
    <h2>
      <a href="/posts/{{post.post_id}}">{{post.post_title}}</a>
    </h2>
    <p>Created by {{post.user.username}} on {{format_date post.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.content}}
    </p>
  </div>
</div> --}}

<div>
  <div>
    <h2>{{post.post_title}}</h2>
    <p>{{post.content}}</p>
    <p>Created by {{post.user.username}} on {{format_date post.created_at}}</p>
  </div>
  <div>
      <p>{{post.comment.comment_body}}</p>
  </div>
  <div>  
    <p>
      <textarea class="comment"></textarea>
      <button class="btn btn-primary comment-button" id={{post.post_id}}>Add Comment</button>
    </p>
  </div>
  <script src="/js/comment.js"></script>
</div>





